{
  "name": "@ucr/core",
  "version": "0.0.7",
  "license": "Apache-2.0",
  "description": "Core parser, resolver, installer, diff and upgrade planner.",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@ucr/schema": "workspace:*",
    "fflate": "0.8.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
